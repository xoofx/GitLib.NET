using System;

using System.IO;

using NUnit.Framework;

namespace LibGit2.Tests
{
    using static libgit2;
    
    public partial class GraphTests
    {
        [Test]
        public void Test_git_graph_ahead_behind()
        {
            Assert.Fail($"Tests for method `{nameof(git_graph_ahead_behind)}` are not yet implemented");
        }
        
        [Test]
        public void Test_git_graph_descendant_of()
        {
            Assert.Fail($"Tests for method `{nameof(git_graph_descendant_of)}` are not yet implemented");
        }
    }
}
